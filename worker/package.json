{
  "name": "guesssong-buzzer",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object hosting GuessSong's buzzer rooms. Deployed separately from the Next.js app on Vercel.",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "typescript": "^5.3.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.0.0"
  }
}
